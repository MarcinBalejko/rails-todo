<p id="notice"><%= notice %></p>

<h1>All Todos</h1>


<% @items.each do |item| %>
    <h3><%= item.item %>
    <%= link_to 'Edit', edit_item_path(item) %>
    <%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' } %> </h3>   
<% end %>
<br>

<%= link_to 'New Item', new_item_path %>
